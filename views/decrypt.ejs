<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <title>Dekripsi Dokumen</title>

  <!-- Font -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">

  <style>
    body {
      margin: 0;
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      background: linear-gradient(135deg, #0f2027, #203a43, #2c5364);
      font-family: 'Poppins', sans-serif;
    }

    .card {
      background: white;
      width: 380px;
      padding: 30px;
      border-radius: 15px;
      box-shadow: 0 15px 30px rgba(0,0,0,0.25);
      text-align: center;
    }

    .card h2 {
      margin-bottom: 10px;
      color: #333;
    }

    .card p {
      font-size: 14px;
      color: #666;
      margin-bottom: 25px;
    }

    .input-group {
      text-align: left;
      margin-bottom: 15px;
    }

    .input-group label {
      font-size: 13px;
      color: #444;
    }

    .input-group input,
    .input-group select {
      width: 100%;
      padding: 10px;
      margin-top: 5px;
      border-radius: 8px;
      border: 1px solid #ccc;
      font-size: 14px;
    }

    button {
      width: 100%;
      padding: 12px;
      margin-top: 10px;
      border: none;
      border-radius: 8px;
      background: linear-gradient(135deg, #11998e, #38ef7d);
      color: white;
      font-size: 15px;
      font-weight: 600;
      cursor: pointer;
      transition: 0.3s;
    }

    button:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 15px rgba(0,0,0,0.2);
    }

    .footer {
      margin-top: 15px;
      font-size: 12px;
      color: #888;
    }
  </style>
</head>
<body>

  <div class="card">
    <h2>🔓 Dekripsi Dokumen</h2>
    <p>Buka kembali dokumen terenkripsi dengan password yang benar</p>

    <form action="/decrypt" method="POST" enctype="multipart/form-data">

      <div class="input-group">
        <label>File Terenkripsi (.enc)</label>
        <input type="file" name="encryptedFile" required>
      </div>

      <div class="input-group">
        <label>Algoritma Dekripsi</label>
        <select name="algorithm">
          <option value="AES">AES</option>
          <option value="DES">DES</option>
        </select>
      </div>

      <div class="input-group">
        <label>Password Dekripsi</label>
        <input type="password" name="password" placeholder="Masukkan password" required>
      </div>

      <button type="submit">Dekripsi</button>
    </form>

  </div>

</body>
</html>
